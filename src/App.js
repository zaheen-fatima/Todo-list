import React from 'react';
import TodoList from './TodoList'; // Make sure TodoList.js is in the same directory

function App() {
    return (
        <div className="App">
            <TodoList />
        </div>
    );
}

export default App;